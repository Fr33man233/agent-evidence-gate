# AEG v0.2.0 账号与工作交接记录

**交接日期：** 2026-08-19  
**用途：** 本地更换 Codex 账号后的继续开发入口。  
**版本范围：** 仅 v0.2.0；不得在本任务中启动 v0.3.0。

## 当前工作位置

- 当前隔离工作树：`C:\Users\win\.codex\worktrees\6ee2\Agent Evidence Gate`
- 交接分支：`codex/v0.2.0-local-handoff`
- 交接基线：`ddea99a5a9860f750151f2e28b45fbd077f27b92`
- 原接续分支：`codex/v0.2.0-omk-native` 仍在另一工作树，保持基线不变；不要强制切换、重置或改写它。
- 公开稳定基线：`v0.1.3` / `d82c7863f48878bfee66e978e7569c464de48ea2`，不得改写。

交接检查点提交只用于本地保存当前工作，不代表已发布、已合并或已完成公开交付。

## 已完成工作

- 原生 OMK EvidenceReceipt v3 parser、JSON duplicate-key/shape/digest/privacy 负例。
- `aeg-task/v2`、`aeg-trace/v1`、Receipt store discovery、goal/check/cwd/trace 绑定。
- OMK-compatible Git workspace fingerprint 与 stale-state 检查。
- E1-only local policy/report v2；`pr`/`protected` 使用 `AEG070` fail-closed。
- 删除 v0.1 custom envelope、adapter、`--evidence` 和 trust upgrade path。
- 保留 release-validation 的历史职责：workflow 固定调用公开 v0.1.3 immutable commit，不让 v0.2 Action 误读 v1 evidence。
- Action 使用冻结设计中的 `repo` input；不再暴露 `repository` 别名。
- CLI `--receipts`、Node 24 Action、可复现 bundle、文档和本地三步 pilot。

## 最后验证证据

- TypeScript 检查通过。
- 完整测试 24/24 通过。
- 两次 `pnpm run build` 字节一致：
  - `dist/aeg.cjs`：318,438 bytes，SHA-256 `20c8d2880a39c9c2f160bda8475c1732901cc17c74d2e8057bf651c0a2e71b69`
  - `dist/action.cjs`：317,678 bytes，SHA-256 `c3ab03c3e8e257c6274c972bf437466711c1b60f31e51bf854a4798bf00c14a2`
- Markdown 相对断链 0；runtime 边界搜索 0 项违规。
- 详细证据：[v0.2.0 RC 审计](docs/reviews/2026-08-19-v0.2.0-rc-security-privacy-determinism.md)。

## 新账号接续步骤

1. 确认已登录正确的 Codex 账号，但不要把 token、API key、环境变量或 secrets 写入项目、prompt、日志或交接文件。
2. 在项目根目录执行：

   ```powershell
   git switch codex/v0.2.0-local-handoff
   git status --short
   ```

3. 若当前环境没有依赖，使用 `pnpm install --frozen-lockfile --ignore-scripts`；不要执行候选项目的 package/script/test。
4. 继续开发前阅读：
   - `docs/superpowers/specs/2026-08-19-native-omk-receipt-v3-design.md`
   - `docs/superpowers/plans/2026-08-19-native-omk-receipt-v3-implementation.md`
   - `docs/superpowers/reviews/2026-08-19-v0.2.0-senior-full-stack-preflight.md`
   - `docs/reviews/2026-08-19-v0.2.0-rc-security-privacy-determinism.md`
5. 修改代码前先新增失败测试；每个切片跑针对性测试，最终重新跑类型检查、构建、完整测试、链接和安全边界检查。

## 权限交接边界

### 允许

- 在交接分支及其工作树内读写源代码、测试、文档和本地构建产物。
- 使用本地 Node 24、pnpm、TypeScript、esbuild、Git 只读状态命令和受限 Git fingerprint 调用。
- 运行合成 fixture、静态检查和本地测试。

### 未授权

- 读取、复制、打印或转移任何 secrets、token、API key、个人数据或私有仓库内容。
- push、PR、Release、tag、Marketplace 更新、main 或公开 v0.1.3 修改。
- 使用 `pull_request_target`、运行 candidate PR command/package/test/script、网络、LLM/API 或运行时安装依赖。
- 将本地 Receipt、trace、report 或真实仓库内容加入提交；只保留已审查的 sanitized fixture。

## 尚未满足的外部门槛

v0.2.0 当前可作为本地实现继续开发，但公开发布仍为 NO-GO：real maintainer CI/E2、第三方安全审计和外部试点没有验证。任何放宽 E1、恢复兼容层或扩大权限的决定都必须由用户在新任务中明确授权。

## 账号切换安全说明

本记录不包含凭据，也不执行账号登出/登入。切换账号应在 Codex 客户端自身完成；切换后只需恢复本地交接分支并重新验证，不需要把任何旧账号权限复制给新账号。
