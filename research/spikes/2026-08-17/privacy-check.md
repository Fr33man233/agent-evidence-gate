# Privacy and Determinism Checks

## Generated artifact scan

The scan covered `reports.json`, `measurements.json`, and `summary.json` generated by the second full run.

| Check | Result |
| --- | --- |
| `AEG_PRIVATE_SENTINEL` in generated reports | 0 matches |
| `command`, `argv`, `cwd`, `stdout`, `stderr`, `prompt` fields in public report objects | 0 matches |
| secrets/tokens in generated artifacts | 0 matches; only synthetic hashes and IDs |
| model calls | 0 |
| network calls | 0 |
| repeated canonical output for I13 | equal |
| alternate unknown-field order for I12 | equal |

The sentinel intentionally exists in I10 input to test the boundary. Its presence in `fixtures.json` and the assertion code is expected and is not public report leakage.
