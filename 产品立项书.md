# **项目立项书**

## **Agent Evidence Gate**

*面向 AI 编程代理交付的可复现、隐私优先证据门禁*

| **文档属性** | **当前值** |
| --- | --- |
| 文档版本 | v0.10-draft（V0 功能合同闭合后） |
| 项目阶段 | v0：立项、问题验证与技术可行性定义 |
| 文档状态 | 动态文档；v0 完成前持续修订 |
| 评审视角 | 产品经理 / 项目经理 / 全栈与安全设计 |
| 最近更新 | 2026-08-17 |
| 下一决策门 | 按 v0.1 MVP 合同完成实现、测试、公开仓库和可复现发布 |

| **当前立项结论**  `PIVOT_ACCEPTED + BOOTSTRAP_CONDITIONAL_GO`：用户决定暂缓前置采用调研，先交付公开 MVP；离线 Spike 已验证跨 harness 映射、外部 assurance 逻辑、只读门禁和最小报告，真实 CI/E2 仍为 `unverified`。允许进入 v0.1 实现，但不得宣传真实 E2 已完成。 |
| --- |

详细策略、威胁模型和产品验收基线集中维护在 [V0 可评审规格包](./v0-specification.md)；Spike 边界以 [互操作 Spike 合同](./spike-contract.md) 为准，完整功能闭合结论见 [V0 功能闭合评审](./v0-closure-review.md)。立项书保留产品与项目决策，不重复维护底层字段细节。

# 1. 立项摘要

Agent Evidence Gate 面向使用 Codex、Claude Code、Copilot、Cursor 或其他 AI 编程代理的开源维护者。它不判断代码是否优秀，也不替代人工审查；它验证代理关于“完成、测试通过、修改范围受控、未访问敏感路径、资源未超限”等交付声明，是否得到新鲜、可追溯、机器可读的执行证据支持。

拟议产品以本地 CLI 和只读 GitHub Action 为主要交付面，输入为任务清单、标准化执行轨迹和 Git base/head 差异，输出为确定性的 pass / warn / fail 结果、JSON 报告和适合 PR 审查的 Markdown 摘要。v0.1 的最终判定不使用 LLM。

| **一句话定位**  一个本地优先、隐私优先的确定性证据合同：它不相信代理的自然语言总结，而是核对任务声明、执行轨迹与 Git 事实是否一致。 |
| --- |

# 2. 立项背景与问题定义

## 2.1 背景

AI 编程代理正在从“生成代码建议”转向能够读取仓库、修改文件、执行命令并提交变更的自治工作流。代理在任务结束时通常会生成自然语言总结，但总结是模型对长链执行过程的压缩，不是独立事实源。命令中断、退出码误读、局部测试被概括成全量测试、工作目录错误、过度 mock、修改验证器或范围漂移，都可能造成声明与实际状态不一致。

因此，本项目不把所有不一致统称为“幻觉”。产品处理的是更宽泛且可工程化的问题：声明—证据不一致。它覆盖模型误判、执行不完整、工具信息丢失、环境状态过期、表达过度概括，以及潜在的轨迹伪造。

## 2.2 核心问题

| **问题陈述**  维护者需要在审查或合并代理提交前，低成本回答：代理声称做过的事是否真的有证据？证据是否对应当前代码状态？证据是否来自足够可信的采集边界？ |
| --- |

- 代理说“测试通过”，但没有可定位的命令、范围、退出码或对应 Git 状态。
- 代理声称只修改目标模块，但 diff 还包含依赖、工作流、测试或策略文件。
- 绿色 CI 可能由被代理同步修改的测试、mock 或验证器产生。
- 完整 transcript、prompt、源码和命令输出不适合默认上传或公开。
- 维护者若必须手工重建整个会话，代理节省的时间会被验证成本抵消。
## 2.3 产品目标与非目标

| **类别** | **定义** |
| --- | --- |
| 目标 | 将交付声明转换为可验证的结构化检查；降低维护者重建执行事实的时间；提供可复现、可审计、隐私最小化的报告。 |
| 非目标 | 不证明代码语义正确；不替代人工代码审查；不执行不可信 PR 代码；不自动修复、评论、合并或调用代理。 |
| 成功边界 | 门禁通过仅表示所配置的证据要求得到满足，不表示实现没有缺陷、测试充分或业务目标必然达成。 |

# 3. 用户、场景与待验证需求

## 3.1 首要用户

首要用户是维护公开仓库、使用 AI coding agent 处理 issue 或 PR、但没有专门平台工程团队的 OSS 维护者。次要用户包括小型研发团队的 Tech Lead、安全或平台工程师，以及开发 agent harness 的工具作者。

## 3.2 核心 Job-to-be-Done

当 AI 代理提交一个改动并声称任务完成时，维护者希望在阅读完整 diff 之前，先快速知道：哪些声明有直接证据、哪些被证据反驳、哪些证据缺失，以及是否发生越界、敏感访问或预算异常，从而决定继续审查、要求补证还是拒绝交付。

## 3.3 当前需求信号与假设

| **需求信号** | **拟议需求** | **当前证据状态** | **v0 验证方式** |
| --- | --- | --- | --- |
| “测试通过”缺少范围和证据 | 命令、退出码、测试范围、事件 ID 与 Git HEAD 绑定 | 论坛与相邻工具反复出现；尚未完成目标用户访谈 | 访谈 + 反例夹具 |
| 代理改动超出授权范围 | allowed_paths 与 diff 比对；高风险文件升级严重度 | 相邻项目与社区讨论支持 | 任务样本复盘 |
| 代理可修改测试或验证器 | 测试、workflow、策略、依赖面单独标记；策略来源可信基线 | 明确的安全与 Goodhart 风险 | 威胁模型桌面演练 |
| 隐私与代码上传顾虑 | 本地处理、最少元数据、默认不保留 prompt/源码/原始输出 | 合理强信号，仍需验证可接受报告内容 | 报告字段卡片排序 |
| 门禁流程过重 | 轻量默认配置 + 风险升级，而非全任务强审批 | 社区反馈提示采用阻力 | 5 分钟可配置性测试 |

# 4. 市场与同质化调研

截至 2026-08-14 的公开检索、静态源码审阅以及 Proof Loop、memi、OMK 三项固定版本运行深审显示，“evidence gate / proof of done / execution trajectory”已形成相邻项目群。项目不能宣称是首个此类工具；差异化必须来自可信边界、标准化数据合同、隐私最小化和维护者工作流体验。以下为已复核的代表性项目，而非完整市场清单。

| **代表项目** | **核心方法** | **与本项目重叠** | **可区分空间** |
| --- | --- | --- | --- |
| Proof Loop | 冻结验收标准、分离 builder/verifier、仓库内 proof artifacts、机械 done gate | 官方测试与机械 gate 运行通过；证据驱动完成门高度重合 | 默认 `check` 未强制时间/Git/producer receipt，report 可原样带出 evidence 文本 |
| Microsoft AgentRx | 将原始轨迹归一为 IR，生成/检查 invariants，再以 LLM judge 定位失败 | 轨迹规范化、约束检查、可审计日志 | 本项目面向 CI 交付门禁，v0.1 最终判定无 LLM |
| OMK | acceptance predicates、v3 verified receipt、可回放 ledger、严格 gate、Git fingerprint | 运行证明 receipt 篡改阻断、选定工件 freshness 与仅 HEAD 漂移阻断，覆盖原技术核心 | 注入 runner 的诚实性仍是信任边界；维护者侧独立 PR verifier、外部 producer assurance 与跨 harness profile 仍可能为空缺 |
| AGILAB | 脱敏 manifest、append-only NDJSON trace、argv hash 和本地 artifact | 隐私优先代理运行证据 | 本项目只做维护者交付门禁，不做广义 AI/ML 运行平台 |
| memi | 低权限 PR Action、结构化报告、严重度阈值 | 运行证明规则门禁与确定性输出成熟 | 检查设计规则而非执行证据；默认写目录、绝对路径可假阴性，报告含源码 excerpt |

| **同质化判断：PIVOT_ACCEPTED。** OMK 已运行覆盖 receipt、旧证据失效、篡改检测与 Git HEAD 状态绑定；原路线已停止。项目只在“运行时无关的 evidence interoperability profile、外部 producer assurance、canonical check/verifier-surface 规则、低权限维护者 PR 门禁与公共最小报告”组合上继续验证。 |
| --- |

完整运行证据、固定版本、负例、隐私测试、成本与差异矩阵见 [2026-08-14 竞品运行级深审](./research/competitor-runs/2026-08-14/run-level-review.md)。

## 4.1 建议定位

英文：Agent Evidence Gate is a deterministic, privacy-preserving evidence contract for AI coding-agent deliveries. It does not decide whether code is good; it verifies whether completion claims are supported by fresh, traceable execution evidence.

中文：Agent Evidence Gate 不判断 AI 写的代码是否优秀，而是验证 AI 关于完成、测试、范围和预算的声明，是否得到新鲜、可追溯、可复现的执行证据支持。

当前收窄定位：它不是新的代理运行时、收据存储或多代理编排器，而是位于多种 agent harness/CI 证据与维护者 PR 工作流之间的互操作 profile 和独立只读 verifier。该定位仍须通过维护者采用验证和最小互操作 spike 成立。

当前执行门禁：`PIVOT_ACCEPTED + BOOTSTRAP_CONDITIONAL_GO + P5 UNVERIFIED`。原 D1/D2/D3 Spike 已取消；按 [v0.1 MVP 合同](./v0.1-mvp-contract.md)进入实现。公开使用价值改为发布后验证，任何 P0 安全/隐私失败仍阻止发布。

# 5. 产品原则与证据可信模型

## 5.1 产品原则

1. 自然语言声明不是事实源；模型输出先视为候选数据。
1. 能由代码确定的 schema、计数、路径、权限、状态和预算，必须由确定性代码处理。
1. 最终 verdict 可复现：相同输入、版本和策略必须得到相同输出。
1. 默认本地、只读和最少数据；公开报告不含 prompt、源码、凭证和原始命令输出。
1. 证据必须新鲜并绑定运行与 Git 状态；过期证据不得静默复用。
1. 通过表示“满足证据合同”，不扩张为“证明代码正确”。
## 5.2 声明层与证据保障等级

自然语言声明与执行证据属于不同对象，不再使用容易混淆的“T0 证据”表述。v0.1 不解析任意自然语言总结；自然语言只供人阅读，不进入确定性 verdict。

| **声明层** | **内容** | **v0.1 产品处理** |
| --- | --- | --- |
| C0 自然语言声明 | 代理最终总结，例如“所有测试已通过” | 不解析、不作为证据、不参与 verdict |
| C1 结构化要求 | manifest 中预先声明的 required checks、范围和预算 | 作为门禁必须核对的产品要求 |
| C2 结构化完成声明 | 可选的 claim ID 与 check ID 关联 | 仅按结构化 ID 核对，不理解自由文本语义 |

| **保障等级** | **证据来源** | **能证明什么** | **不能证明什么** | **产品处理** |
| --- | --- | --- | --- | --- |
| E0 无证据 | 只有声明，没有可核对记录 | 代理声称了什么 | 事件是否发生 | 必需项不得通过 |
| E1 自报记录 | 代理或普通适配器生成 JSONL | 记录内部是否完整、一致 | 记录是否被遗漏或伪造 | 本地预检查可接受；正式 PR 默认不足 |
| E2 绑定收据 | 代理控制边界之外的采集器记录命令、退出码、时间和 Git 状态 | 某命令在特定代码状态下执行并产生结果 | 测试是否充分、代码语义是否正确 | 正式 PR 门禁的默认最低保障 |
| E3 独立验证 | 可信基线策略 + 隔离 CI / 独立 verifier | 候选变更通过独立检查 | 未配置属性的整体正确性 | 发布和高风险变更的增强方向 |

| **已确认 P0 决策**  本地预检查允许 E1；正式 PR 门禁通过默认要求 E2。只有 E2 及以上才可以使用“证明执行发生”等强表述；若实际只能提供 E1，产品必须明确称为“自报记录一致性检查”。 |
| --- |

# 6. v0 阶段定义

## 6.1 v0 目标

v0 是立项与可行性验证阶段，不交付可运行产品。目标是把一个有吸引力的方向收敛为可开发、可验收、不会误导用户的产品和技术合同，并对是否进入 v0.1 给出明确 GO/NO-GO。

## 6.2 v0 范围

| **工作流** | **核心任务** | **交付物** | **负责人角色** |
| --- | --- | --- | --- |
| A. 问题与用户验证 | 完成目标用户画像、3-5 次半结构访谈或等价公开案例复盘；验证痛点频率、严重度、现有替代方案和采用阻力 | 研究记录、需求证据表、假设更新 | 产品 |
| B. 竞争与定位 | 建立竞品矩阵；试用/审阅 3-5 个高度相关项目；明确不重复造轮子的边界 | 竞品矩阵、差异化声明、继续/转向建议 | 产品 + 技术 |
| C. 证据与信任模型 | 定义 C0-C2 与 E0-E3；明确攻击者、可信组件、证据新鲜度和可伪造面 | 信任模型、威胁模型、承诺边界 | 安全 + 架构 |
| D. 数据与策略契约 | 冻结 manifest、trace、report 的字段草案；定义 verdict、check ID、severity 和 evidence pointer | 三份 schema 规格草案、字段字典、样例 | 架构 |
| E. 最小门禁规格 | 定义六项初始检查、严重度、失败条件、例外和补救文案；决定 backed/contradicted/unbacked 映射 | 策略目录与判定表 | 产品 + 架构 |
| F. 技术路线决策 | 比较 TypeScript/Python、Action 分发、Git diff 获取、schema/YAML 库、路径匹配和跨平台约束 | ADR、依赖候选、一次性技术 spike 计划 | 全栈 |
| G. 测试与退出评审 | 定义正负夹具、性能/隐私/安全阈值和 v0.1 资源预算 | 夹具矩阵、验收计划、GO/NO-GO 记录 | 项目管理 |

## 6.3 v0 明确不做

- 不编写生产 CLI、GitHub Action 或正式适配器；技术 spike 如获批准必须可丢弃且不形成产品依赖。
- 不建设 Dashboard、Web 后台、账户系统、数据库或云端控制面。
- 不运行大规模真实 Codex benchmark，不在 PR 默认路径发起模型调用。
- 不实现自动修复、自动评论、自动合并或多代理编排。
- 不声称证明代码正确，也不将同一代理生成的自报记录包装成不可伪造证据。
- 不在 v0 冻结 v0.2/v0.3 的详细功能，只保留方向、依赖和决策触发条件。
## 6.4 v0 优先级

| **优先级** | **事项** | **原因** |
| --- | --- | --- |
| P0 | 证据可信边界、隐私与 GitHub Action 安全模型 | 错误承诺会使整个产品失去可信度，并可能造成 secret 或供应链风险 |
| P0 | 三份数据契约与确定性 verdict 语义 | 所有实现、适配器和测试的共同基础 |
| P0 | 同质化与目标用户采用验证 | 避免实现已经被更成熟项目覆盖的薄功能 |
| P1 | 技术栈、打包、跨平台和性能路线 | 决定 v0.1 的交付速度与维护成本 |
| P1 | 最小策略与负例夹具矩阵 | 让规格可验收，降低开发返工 |
| P2 | 品牌、官网、演示视觉和 Fund 申请文案 | 不影响 v0.1 技术可行性，完成证据后再做 |

## 6.5 已批准的产品默认值

以下决定由产品负责人于 2026-08-13 确认，后续规格和技术验证按此执行：

1. v0.1 不解析任意自然语言总结；只核对 manifest 要求与可选结构化声明。
1. 本地预检查允许 E1；正式 PR 合并门禁默认要求 E2。
1. 同一代理生成、修改并运行测试时标记为 `self_verified`，不得表述为独立验证。
1. 证据必须绑定 Git 状态；代码状态变化后旧证据立即失效。
1. 测试、workflow、policy 或 verifier 被修改时默认产生警告；受保护分支可要求人工批准。
1. 缺失资源数据在普通 profile 中为警告，在预算强制 profile 中为失败；缺失值不得按零计算。
1. 只有 E2 及以上才能使用“证明真实执行”等强产品表述。
1. 首发用户为 OSS 维护者；本地 CLI 是核心能力，GitHub Action 是主要交付面。
1. `pass` 只表示证据合同满足，不表示代码正确、测试充分或可以自动合并。

# 7. v0 数据契约草案

## 7.1 agent-task manifest

定义任务目标、基准提交、允许路径、必需命令、敏感路径、依赖/锁文件策略、资源预算及显式例外。v0 要解决字段必填性、glob 语义、路径规范化、命令身份和策略版本问题。

| **字段组** | **候选字段** | **关键决策** |
| --- | --- | --- |
| 身份与版本 | schema_version, task_id, policy_version | 版本兼容与稳定 ID |
| Git 绑定 | repository, base_commit, expected_head? | 证据新鲜度与变更后失效 |
| 范围 | allowed_paths, denied_paths, exceptions | 优先级、路径穿越、大小写和符号链接 |
| 验证 | required_checks: id, kind, argv, cwd, success_exit_codes, scope | 稳定 check ID + 规范化 argv/cwd；不解析自然语言命令声明 |
| 隐私 | sensitive_paths, report_redaction | 默认策略及用户覆盖边界 |
| 预算 | max_tokens, duration, tool_calls, retries | 缺失用量、单位和聚合规则 |

## 7.2 agent-trace JSONL

事件采用 append-only、schema-versioned、单行 JSON。v0.1 候选事件为 run_started、file_read、file_written、command_finished、test_result、model_usage、retry、run_finished。每条事件至少包含 run_id、event_id、sequence、timestamp、event_type 和结构化 metadata。`command_finished` 必须包含 check ID、规范化 argv/cwd、termination、显式 exit code（取消/超时可为 null）、stdout/stderr 分离摘要和 Git state；原始输出不进入 trace。测试统计为可选增强，一旦提供则必须完整给出 passed/failed/skipped/cancelled。

| **隐私约束**  契约默认排除 prompt、源码内容、凭证和原始命令输出。若需要证明输出，可优先记录稳定摘要、计数、退出码和受约束 hash；hash 也不得被误解为内容安全。 |
| --- |

## 7.3 gate-report

报告包含 schema/policy 版本、输入 Git 标识、逐项 check ID、severity、claim status、evidence event IDs、资源总计、证据保障等级和可执行的 remediation。总体结果不再用单一字段混合“策略符合”与“证据可信”，至少区分：

- `policy_verdict`：现有记录是否满足配置策略；
- `assurance_level`：当前证据实际达到 E0-E3 中的哪一级；
- `required_assurance`：当前 profile 要求的最低保障；
- `gate_verdict`：综合策略与保障后是否允许通过；
- `self_verified`：测试是否由同一代理生成/修改并在同一执行边界内运行。

报告必须显式呈现局限：证据通过不等于语义正确。

# 8. 最小门禁功能规格（v0 待冻结）

| **检查** | **主要输入** | **最小判定** | **初始结果建议** |
| --- | --- | --- | --- |
| Scope | manifest + git diff | 所有 changed path 均被允许或有有效例外 | 越界 fail；高风险文件单独提升 |
| Required commands | manifest + trace | 每个要求均有匹配且新鲜的 command receipt | 缺失 unbacked/fail |
| Test evidence | claim/requirement + trace | 测试命令存在、退出码 0、范围不被夸大 | 矛盾 fail；范围不明 warn |
| Sensitive paths | policy + file events | 读取/写入敏感路径被阻止或显式报告 | 按策略 fail/warn |
| Dependency integrity | diff + policy | manifest 变化满足 lockfile 和高风险文件规则 | 不满足 fail |
| Resource budget | manifest + usage events | token、时长、工具调用和重试在预算内且聚合完整 | 超限 fail；普通 profile 缺失为 warn；预算强制 profile 缺失为 fail |
| Trace integrity | trace + Git identity | schema 完整、序列有序、大小受限、无禁存字段、证据未过期 | 完整性错误 fail |
| Test surface | git diff + policy | 测试、fixture、snapshot、mock、测试配置是否变化 | 默认 warn；受保护分支可要求人工批准 |
| Verifier surface | git diff + trusted policy | workflow、policy、verifier、门禁自身是否变化 | 默认 warn；受保护分支可要求人工批准 |
| Assurance | receipt provenance + profile | 实际保障等级是否达到 profile 要求 | 本地 E1 可通过；正式 PR 低于 E2 则 fail |

# 9. 技术路线选择

## 9.1 目标架构

架构采用端口—适配器思路：供应商适配器只负责把原始日志转成规范化事件；核心包负责 schema、Git 事实和策略判定；报告渲染器负责终端、JSON 与 Markdown；GitHub Action 只是调用核心 CLI 的只读交付面。供应商字段不得进入核心策略。

| **核心边界**  Adapter / fixture → normalized trace；manifest + trace + Git facts → deterministic policy engine；verdict → CLI / JSON / Markdown / Action。LLM 不进入 v0.1 的必经判定路径。 |
| --- |

## 9.2 语言与分发方案

| **候选** | **优势** | **代价/风险** | **v0 建议** |
| --- | --- | --- | --- |
| TypeScript | GitHub Action 生态和单 bundle 分发自然；跨平台 Node 普遍；JSON Schema 工具成熟 | YAML/路径/glob 细节需严格约束；构建产物维护；类型不替代运行时 schema | 暂列首选；通过 1 个可丢弃 spike 验证 bundle、Windows/Linux、退出码和 Action 输入 |
| Python | 数据处理、CLI、schema 与测试开发快；可读性好 | Action 需确保 Python 环境与依赖安装；分发/启动摩擦可能更高 | 作为备选；若目标用户更重本地 CLI 或 Python 贡献者生态则重新评估 |

当前技术建议不是最终选型：以最小安装摩擦、确定性行为、跨平台一致性、供应链面、维护成本和可测试性为评分维度。v0 只批准可丢弃验证，不开始生产实现。

## 9.3 GitHub Action 安全路线

- 优先使用 pull_request 的受限权限模型；明确 permissions: contents: read。
- 不使用 pull_request_target 检出并执行 fork 代码；若未来确有需要，必须进行独立安全评审。
- v0.1 Action 只把 PR 内容当作不可信数据检查，不执行 npm install、构建或测试 PR 代码。
- 限制输入大小、JSON 深度、事件数量、路径长度和报告输出，防止资源耗尽。
- 固定第三方 Action 版本/提交；不把 secrets、原始输出或私有代码写入 artifact。
# 10. 威胁模型与隐私基线

| **威胁** | **影响** | **v0 需要定义的控制** | **剩余风险** |
| --- | --- | --- | --- |
| 代理伪造/省略轨迹 | 错误 pass | 保障等级、完整性字段、采集器边界、默认降级 | E1 无法排除此风险 |
| 证据过期或与 Git 状态不符 | 旧测试为新代码背书 | base/head SHA、dirty tree 标识、变更后失效 | 外部依赖仍可能漂移 |
| 修改测试/验证器/策略 | 绿色结果被操纵 | 高风险文件标记；从可信 base 读取策略的后续路径 | 仍需人工判断语义 |
| 路径穿越/符号链接/大小写差异 | 越界绕过 | 规范化、仓库根边界、平台测试 | 复杂文件系统差异 |
| 恶意超大 JSONL | CI DoS | 流式解析、大小/事件上限、超限失败 | 资源阈值需实测 |
| secret 或源码泄漏 | 隐私/供应链事故 | 禁存字段、脱敏、最少报告、扫描负例 | hash/文件名亦可能敏感 |
| fork PR 中执行不可信代码 | 凭证泄漏或 runner 接管 | 只读检查、不执行 PR 代码、不使用 secrets | 未来独立 verifier 需隔离 |

# 11. v0 验证与夹具计划

| **场景** | **预期** | **验证目的** |
| --- | --- | --- |
| 完整且范围内的可信收据 | pass | 基准正例 |
| 声称测试通过但没有命令事件 | fail / unbacked | 拒绝无证据声明 |
| 测试事件退出码非 0，但总结称通过 | fail / contradicted | 识别直接矛盾 |
| 仅局部测试通过却声称全量通过 | warn 或 fail | 范围语义 |
| 修改越界、路径穿越、大小写变体 | fail | 路径策略 |
| 读取 .env / secrets 路径 | fail 或策略化 warn | 敏感访问 |
| 依赖清单变化但锁文件缺失 | fail | 依赖完整性 |
| 预算超限或用量事件缺失 | fail / warn 待决 | 资源策略 |
| 事件重复、序号断裂、run_id 混合 | fail | 轨迹完整性 |
| 证据绑定旧 HEAD | fail / stale | 证据新鲜度 |
| 报告含疑似 secret 或原始输出 | fail | 隐私基线 |
| 代理修改测试/workflow/policy | 高风险提示或 fail | 验证器可变性 |

# 12. 项目计划、资源预算与治理

## 12.1 v0 时间盒

| **周期** | **重点** | **退出产物** |
| --- | --- | --- |
| 第 1-2 天 | 整理假设、竞品清单、访谈脚本、声明层与证据保障模型 | 研究计划与 C0-C2、E0-E3 草案 |
| 第 3-5 天 | 访谈/案例复盘、竞品深审、需求优先级 | 证据表、竞品矩阵、定位更新 |
| 第 6-8 天 | 数据契约、策略目录、威胁模型、夹具矩阵 | 可评审规格包 |
| 第 9-10 天 | 技术路线评分、风险与资源评审、GO/NO-GO | v0 评审记录与 v0.1 实施授权建议 |

建议时间盒为 5-10 个聚焦工作日、约 20-35 小时；若无法获得维护者访谈，可用至少 5 个公开 issue/讨论和 3 个相邻项目的实际试用作为降级证据，但必须降低市场结论置信度。

## 12.2 资源效率预算

| **资源** | **当前控制** | **预期改进** | **验证方法** |
| --- | --- | --- | --- |
| 模型调用 | 以经济型模型完成整理；强模型只用于最终架构/安全门评审；v0 不跑 live benchmark | 避免把调研成本变成持续 API 成本 | 记录用途与调用次数/成本；复用已验证研究 |
| 开发返工 | 先冻结信任模型、schema 与退出语义，再批准实现 | 降低核心合同变化导致的重写 | v0.1 中 schema/策略破坏性变更次数 |
| 测试计算 | 优先固定夹具和 deterministic replay | 默认 PR 无模型、无真实构建运行 | 单次回放耗时、重复结果一致性 |
| 维护成本 | 一个语言、一个核心引擎、适配器隔离、无数据库 | 缩小依赖和发布面 | 依赖数量、发布步骤、跨平台矩阵 |

## 12.3 决策与变更治理

- 本立项书是单一产品基线；每次范围或核心语义变化需更新版本、日期、决策理由和影响。
- v0 期间新增需求先进入假设池，只有与目标用户和最近外部里程碑直接相关、且有证据支持，才可进入 P0/P1。
- 任何扩大外部写入、执行不可信代码、上传源码/prompt 或引入 LLM 最终裁判的提议，必须重新进行安全、隐私和成本评审。
- 后续版本详细功能在 v0 完成后补充；当前只记录方向，不形成承诺。

## 12.4 文档与任务组织

- `产品立项书.md` 是 V0 期间的日常维护单一事实源；`产品立项书.docx` 暂作为 2026-08-12 的排版快照，待关键里程碑再统一导出，避免双份文档逐次同步造成重复劳动和内容漂移。
- `v0-specification.md` 是 V0 技术合同的单一事实源，集中维护数据契约、策略规则、威胁、夹具和 spike 计划；立项书只保留摘要与链接。
- 当前 Codex 任务继续负责完成 V0：产品决策、数据契约、策略表、威胁模型、夹具矩阵、技术 Spike 说明与 bootstrap 路线决策。
- V0 中若执行技术 spike，其代码必须位于明确的临时/实验范围，可丢弃、不作为正式产品基线；是否实际执行须先完成 spike 验收设计。
- v0.1 实现必须以 [v0.1 MVP 合同](./v0.1-mvp-contract.md)为输入，建议另开 Codex 任务隔离正式代码与前期探索上下文。
- v0.1 仍须经过独立的 P0 安全、隐私和发布验收；bootstrap 决策不授权跳过这些门禁。
# 13. v0 退出标准与 GO/NO-GO

| **退出标准** | **通过条件** |
| --- | --- |
| 问题成立 | 本阶段明确标记为 `DEFERRED`；发布后使用信号替代前置访谈，不把缺失证据写成已验证 |
| 差异化成立 | 能用可验证能力区分至少 3 个高度相关项目，不依赖“第一个”宣传 |
| 信任模型成立 | C0-C2 与 E0-E3、正式 PR 最低 E2、威胁主体、可信组件、可伪造面和产品承诺一致 |
| 契约可实现 | manifest、trace、report 字段草案完整；版本、Git 绑定、证据指针、缺失语义明确 |
| 策略可验收 | 每项检查有输入、算法边界、结果、严重度、负例和补救建议 |
| 隐私安全可接受 | 公开报告最少化；fork PR 不使用 secrets、不执行不可信代码；资源上限明确 |
| 技术路线可交付 | 语言/分发选择有评分证据；跨平台、Action、安全与依赖风险有应对 |
| 资源预算可控 | v0.1 不依赖 LLM；默认 PR 走固定夹具/静态检查；开发与测试预算被批准 |

| **Bootstrap GO 条件**  用户明确接受延后采用验证；技术 Spike 达到 `CONDITIONAL_GO`；v0.1 MVP 合同冻结；允许进入实现，但不代表市场或 Fund 申请已成立。 |
| --- |

| **NO-GO / 转向条件**  无法建立超过 E1 的可信边界却继续宣传“真实证据”；目标用户不愿承担配置成本；差异化只剩品牌或 UI；或者 GitHub Action 必须执行不可信代码才能提供核心价值。 |
| --- |

# 14. 后续版本方向（暂不冻结）

以下仅用于保持路线连续性，详细需求、优先级和验收标准将在 v0 退出后重新立项。

| **阶段** | **暂定意义** | **进入条件** |
| --- | --- | --- |
| v0.1 Evidence Gate | 交付完整 V0 功能面、CLI + 只读 Action，证明证据合同和确定性门禁可工作 | `BOOTSTRAP_CONDITIONAL_GO` |
| v0.2 Cost Evidence | 在可信轨迹基础上，增加 token、时长、工具调用、重试和异常循环证据 | v0.1 有真实使用且资源字段稳定 |
| v0.3 Cost Regression | 用 3-5 个公开维护夹具比较成功率、成本、时长和工具调用 | v0.2 统计可复现；live run 成本预算批准 |
| Fund 申请 | 以公开仓库、Action、隐私模型、真实 OSS 报告和 credits 使用计划申请支持 | MVP 可公开复现；使用数据和限制披露完整 |

# 15. 当前决策与剩余工作

V0 功能合同已按 [V0 功能闭合评审](./v0-closure-review.md) 关闭，以下不再作为开放问题：

- v0.1 使用 TypeScript，发布固定版本 Node bundle；
- 首发 adapter 为 OMK v3 与 maintainer CI envelope；
- E2 采用外部 trust context 候选路径，真实生产证明仍为 `unverified`；
- state binding 采用兼容性验证，不重新实现 OMK receipt hash、ledger 或独立 replay 数据库；
- 输入上限、C0-C2/E0-E3、local/pr/protected、F01-F28 和 I00-I15 已纳入 v0.1 必做范围。

剩余工作只有实现和发布验收：核心引擎、两个 adapter、CLI、只读 Action、完整夹具自动化、独立 P0 review、公开 synthetic demo、release 与 rollback。
# 附录 A：公开来源与调研边界

本版市场判断来自公开检索与对代表性项目的快速复核，不等于法律意义上的完整竞品或专利检索。社区帖子用于发现需求信号，不作为发生率或市场规模的统计证据。建议 v0 对关键项目完成安装/运行级别验证并记录日期、版本与实际差异。

- [Proof Loop — repo-local verification protocol](https://github.com/LeoStehlik/proof-loop)
- [Microsoft AgentRx — diagnosing failures from execution trajectories](https://github.com/microsoft/AgentRx)
- [OMK — evidence-gated coding-agent control plane](https://github.com/dmae97/omk)
- [AGILAB — redacted agent-run manifests and NDJSON traces](https://github.com/ThalesGroup/agilab)
- [memi — low-permission pull-request Action pattern](https://github.com/memi-design/memi)
- [GitHub Docs — Securely using pull_request_target](https://docs.github.com/en/actions/reference/security/securely-using-pull_request_target)
- [GitHub Docs — Secure use reference](https://docs.github.com/en/actions/reference/security/secure-use)
- [Codex for Open Source / OSS program form](https://openai.com/form/codex-for-oss/)
- [Codex Open Source Fund form](https://openai.com/form/codex-open-source-fund/)
# 附录 B：文档修订记录

| **版本** | **日期** | **主要变化** | **状态** |
| --- | --- | --- | --- |
| v0.1-draft | 2026-08-12 | 整合项目定义、市场调研、需求、技术路线、v0 范围、优先级、信任模型和 GO/NO-GO 门槛 | 历史 |
| v0.2-draft | 2026-08-13 | 确认产品默认值；拆分声明层与证据保障；明确 E2 正式 PR 门槛、self_verified、验证面变化、资源缺失规则及任务拆分 | 历史 |
| v0.3-draft | 2026-08-13 | 建立集中式 V0 可评审规格包；补齐数据契约、策略表、威胁模型、28 个夹具和技术 spike 计划 | 历史 |
| v0.4-draft | 2026-08-13 | 建立用户/竞品证据协议与台账；静态深审 Proof Loop、AgentRx、OMK、AGILAB、memi；同质化风险升级为 PIVOT_WATCH 并收窄定位 | 历史 |
| v0.5-draft | 2026-08-13 | 完成 PRE-SPIKE full-stack 评审、环境基线、分道门禁、共享 fixture、测量/ADR 模板、资源预算和停止条件 | 历史 |
| v0.6-draft | 2026-08-14 | 完成 Proof Loop、memi、OMK 固定版本运行深审；确认 OMK 覆盖 receipt/freshness/Git state 核心；结论升级为 PIVOT_RECOMMENDED 并暂停原 Spike | 历史 |
| v0.7-draft | 2026-08-17 | 用户接受项目转向；取消原 D1/D2/D3，冻结跨 harness 互操作、外部 assurance、只读 verifier 与最小报告 Spike 合同 | 历史 |
| v0.8-draft | 2026-08-17 | 完成 I00-I15 离线互操作 Spike；4 个 pass、12 个 fail-closed；真实 CI/E2 未验证，出口为 CONDITIONAL_GO | 历史 |
| v0.9-draft | 2026-08-17 | 用户选择跳过前置采用调研，冻结 bootstrap MVP 路线和 v0.1 合同，允许进入实现 | 历史 |
| v0.10-draft | 2026-08-17 | 恢复完整 V0 功能要求，完成阈值、契约、28 个夹具和 Action 安全闭合评审 | 当前 |
