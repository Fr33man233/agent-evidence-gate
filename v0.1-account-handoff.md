# AEG v0.1 Account-Switch Handoff

> Prepared: 2026-08-17; updated 2026-08-18  
> Status: v0.1.0 is published publicly; v0.1.1 is a release-validation candidate. Real CI/E2 and independent review remain pending.

## Resume instruction

Open `C:\Users\win\Documents\ChatGPT\Agent Evidence Gate` in Codex, then paste:

> Continue AEG v0.1 from `v0.1-account-handoff.md`. Read `v0.1-mvp-contract.md`, `v0-closure-review.md`, `v0-specification.md`, and `v0.1-preflight-review.md` before editing. Preserve the published scope and existing files. Do not claim real CI/E2 or independent security assurance without new evidence.

## Frozen decisions

- v0.1 is a full V0-surface MVP, not a reduced spike.
- Runtime LLM/API/network usage is zero. Candidate PR code, manifest commands, packages, tests, and scripts are never executed.
- No secrets, `pull_request_target`, receipt hash, ledger, replay database, or new OMK control plane.
- Architecture: `adapter -> canonical profile -> policy engine -> report renderer`.
- Supported adapters: OMK v3 and maintainer-CI envelope only.
- Real CI/E2 is still `unverified`. `E2-candidate` means only that configured synthetic/available trust facts passed the verifier; it is not a production attestation.
- Public repository is `https://github.com/Fr33man233/agent-evidence-gate`; default branch is `main`; tag `v0.1.0` points to the release commit.

## Completed and verified locally

- Senior full-stack preflight: `v0.1-preflight-review.md` (`GO_FOR_LOCAL_IMPLEMENTATION`).
- Fixed dependency manifest and lockfile: TypeScript 5.9.2, Node types 22.15.21, YAML 2.8.1, esbuild 0.25.9, tsx 4.20.5.
- Bounded YAML manifest and JSONL trace preflight; duplicate keys, traversal, forbidden private fields, size/event limits are rejected.
- Canonical JSON report/Markdown renderer and stable non-pass exit codes.
- Canonical adapter/evidence model, E1-to-E2 prevention, AEG010/020/021/022/030/031/040/050/060/061/070 policy rules, and structural evidence references.
- Read-only Action skeleton (`action.yml`, `src/action.ts`) and compatibility fingerprint utilities.
- Manifest now preserves C2 claims, dependency policy, resource budget, test surface, verifier surface and structured exceptions; policy covers enforced budget overruns/missing usage, denied dependency declarations, and test-surface review.
- Git facts/state binding now runs through a shared `src/runner.ts`; CLI requires `--repo`, and Action uses `repository` input or `GITHUB_WORKSPACE`.
- CLI and read-only Action bundles were rebuilt successfully with the pinned esbuild toolchain.
- Synthetic committed-repository integration verifies HEAD, changed paths, dirty state, and compatibility fingerprint.
- Command receipts now require manifest-matching check/argv/cwd/termination/exit fields; stdout/stderr may only be bounded summary objects.
- Profile/test-result semantics now cover local E1 warning, PR/protected E1 failure, skipped warning, and failed/cancelled failure.
- Canonical report repeatability is covered by a byte-identical determinism test.
- Frozen reason-code mapping is now enforced by regression tests: scope AEG010, required check AEG020, sensitive write AEG030, sensitive read AEG031.
- OMK and maintainer-CI now compute equivalent E2-candidate assurance when equivalent independent trust context is supplied; untrusted OMK self-claims remain E1.
- Protected verifier-surface changes now fail without `surface-approved` and warn with that explicit exception; C2 claims match stable IDs only.
- F11/F13/F24 coverage now checks explicit sensitive-read observability, required lockfile presence, and rename crossing allowed/denied scope.
- Full F01-F28 and I00-I15 matrices are executable; CLI integration verifies a committed synthetic repository; AEG022 self-verification and retry-budget regressions are covered.
- Public-ready README, Quickstart, threat model, synthetic demo, MIT license, read-only workflow template, release/rollback guide, and pre-release P0 review are present.
- Last local suite: 75 tests passed; pinned esbuild rebuilt both CLI and Action bundles, and `pnpm exec tsc --noEmit` passed.

## Remaining release boundaries

- Real CI/E2 producer identity remains `unverified`; `E2-candidate` is not production proof.
- Independent external security/privacy review is still required before claiming independently validated release assurance; the local review is recorded as `GO_FOR_LOCAL_MVP_ARTIFACT / NO-GO_FOR_EXTERNAL_RELEASE`.
- Public repository upload, `main` and release branches, and the `v0.1.0` tag are complete. A `v0.1.1` patch candidate addresses the clean-install build-policy issue; no Marketplace publication was performed.

## Files to read first

1. `v0.1-mvp-contract.md` and `v0-closure-review.md` — binding scope and security decisions.
2. `v0.1-preflight-review.md` — this iteration's GO, budget, and stop conditions.
3. `src/` and `tests/` — current implementation only; `spike/` is disposable evidence and must not be copied unreviewed.
4. `research/spikes/2026-08-17/adr.md` — explains why E2 remains unverified.

## Safe local commands

Use the bundled Node runtime if `node` is not in PATH:

```powershell
$node = 'C:\Users\win\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe'
& $node --import tsx --test tests\preflight.test.ts tests\policy.test.ts tests\action-security.test.ts tests\git.test.ts
```

No credentials, tokens, private source, or user data were placed in this handoff or the current synthetic fixtures.
