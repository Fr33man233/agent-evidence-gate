# AEG 项目接续说明

> 首次准备：2026-08-17
>
> 最近更新：2026-08-19
> 当前状态：v0.1.3 已公开发布并上架 GitHub Marketplace；受控 synthetic CI 验证以 `E2-candidate` 通过。v0.2.0 原生 OMK Receipt v3 设计已批准，但功能实现尚未开始。

## 1. 接续指令

在 Codex 中打开 `C:\Users\win\Documents\ChatGPT\Agent Evidence Gate`，优先阅读：

1. [版本历史与档案审计](docs/version-history.md)；
2. [v0.2.0 原生 OMK Receipt v3 设计](docs/superpowers/specs/2026-08-19-native-omk-receipt-v3-design.md)；
3. [v0.1.3 发布记录](docs/releases/v0.1.3-release-record.md)；
4. [v0.1 MVP 合同](v0.1-mvp-contract.md)、[V0 功能闭合评审](v0-closure-review.md)和 [V0 规格](v0-specification.md)，用于理解历史约束。

继续开发时应保留公开 v0.1.3 tag 和 release，不得把 v0.2.0 设计写成已实现，也不得在没有新证据时宣称 real CI/E2 或独立安全保障已经完成。

## 2. 已发布基线

- 公开仓库：<https://github.com/Fr33man233/agent-evidence-gate>
- 默认分支：`main`
- 最新公开版本：`v0.1.3`
- 公开 tag target：`d82c7863f48878bfee66e978e7569c464de48ea2`
- GitHub Marketplace：已发布
- v0.1.3 受控 run：[`32211203587`](https://github.com/Fr33man233/agent-evidence-gate/actions/runs/32211203587)，成功
- 报告结论：`pass / E2-candidate`；这不是 production-proven independent CI identity
- 最近复验基线：76项测试通过、0项失败

本地 `v0.1.1`、`v0.1.2` tag 与远端公开 target 不一致。不得强制改写这些 tag；验证历史发布时使用 [版本历史](docs/version-history.md)记录的远端 commit SHA。

## 3. v0.1 已完成能力

- TypeScript CLI 与只读 GitHub Action；固定 Node bundle。
- `agent-task.yml`、`agent-trace.jsonl` 和 v0.1 evidence envelope 输入契约。
- bounded YAML/JSONL 预检、Git facts/state binding、C0-C2、E0-E3、local/pr/protected profiles。
- scope、敏感路径、依赖、资源 budget、test surface、verifier surface 与稳定 reason codes。
- canonical JSON/Markdown report、稳定退出码和确定性测试。
- F01-F28、I00-I15、CLI committed-repository integration、Action 静态安全与隐私边界测试。
- OMK v3 envelope adapter 与 maintainer-CI envelope adapter。
- 运行时 LLM/API/network 为 0；不执行 candidate PR 代码、命令、package、test 或 script。

v0.1 的完整冻结范围仍以 [v0.1 MVP 合同](v0.1-mvp-contract.md)为准。

## 4. v0.2.0 已批准边界

- 当前分支：`codex/v0.2.0-omk-native`
- v0.2.0 是破坏性迭代，不保留 v0.1 自定义 evidence envelope 兼容层。
- 直接读取原生 OMK EvidenceReceipt v3，不重新实现 OMK receipt hash、ledger、replay database 或执行控制面。
- 原生 OMK Receipt 在 v0.2.0 中最高限定为 E1；版本目标是先形成可工作的本地门禁。
- 保持只读、离线、确定性、无 secrets、无 candidate execution 的安全边界。
- Action 计划迁移到 Node 24；公开 v0.1.3 仍为 Node 20。
- 当前只完成设计与版本前审查，尚未开始 schema、adapter、policy、CLI/Action 或测试实现。

所有实现选择、删除项、数据合同、资源预算和退出标准以 [v0.2.0 设计文档](docs/superpowers/specs/2026-08-19-native-omk-receipt-v3-design.md)为准。

## 5. 仍未完成或不得扩大声明的事项

- real maintainer-controlled producer identity / real E2 仍为 `unverified`。
- `E2-candidate` 仅表示 synthetic/available trust facts 通过 v0.1 verifier，不是 production attestation。
- 独立第三方 security/privacy review 尚未完成；仓库内 focused review 不能改称第三方审计。
- v0.2.0 尚未实现、测试、发布或上架 Marketplace。
- 前置 3–5 名维护者采用调研被用户明确暂缓；后续以公开使用信号验证价值。

## 6. 档案边界

- v0.1.0 至 v0.1.3 的公开事实已汇总到 [CHANGELOG](CHANGELOG.md)和[版本历史](docs/version-history.md)。
- `research/competitor-runs/2026-08-14/run-level-review.md` 未进入当前 Git 历史，也没有可验证副本；不得补造。
- 产品立项书列出的 `v0.1-draft` 至 `v0.9-draft` 没有独立 Git 快照；只能保留现有摘要，不能还原逐版正文。
- v0.1.1 未找到独立成功 controlled run；不得补写测试数、run ID 或 pass verdict。

## 7. 安全本地验证

使用项目依赖中的 `tsx` 运行完整测试：

```powershell
node --import tsx --test "tests/**/*.test.ts"
```

若系统 `node` 不在 PATH，可使用 Codex bundled Node runtime。测试与示例只能使用 synthetic fixtures，不得加入凭据、私有仓库源码、个人数据或 candidate execution。
