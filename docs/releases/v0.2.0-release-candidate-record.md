# AEG v0.2.0 Release Candidate Record

**状态：** 本地 release candidate，未发布。  
**候选版本：** `0.2.0`  
**候选分支：** `codex/v0.2.0-local-handoff`  
**公开基线：** `v0.1.3` / `d82c7863f48878bfee66e978e7569c464de48ea2`，保持不变。

## 已完成的本地发布准备

- package metadata 已从开发分支的公开基线值切换为候选版本 `0.2.0`；没有改写公开 tag、Release 或 Marketplace alias。
- README 保留 v0.1.3 的历史 consumer 示例，明确 v0.2.0 尚未发布，避免消费者误用未发布 Action 输入合同。
- Action 使用 `node24`，输入为 `manifest`、`trace`、`receipts`、`repo`、`json`、`markdown`；运行时不使用 secrets、网络、shell 或候选执行。
- release-validation workflow 继续固定到公开 v0.1.3 immutable commit，只验证历史 v0.1 合同，不冒充 v0.2 验证。
- 本地安全审计已关闭 3 个 P1 输入边界问题；记录见 [v0.2.0 本地安全审计](../reviews/2026-08-19-v0.2.0-security-audit.md)。

## 本地证据

- TypeScript：通过。
- 测试：31/31 通过；版本元数据和历史 workflow 合同测试已加入。
- bundle：同一已有依赖连续构建字节一致。
  - `dist/aeg.cjs`：320,351 bytes；SHA-256 `26a38117c408a94985681c43d489bdd77e1dbcd6279be796273fd06aa66c5f1b`
  - `dist/action.cjs`：319,587 bytes；SHA-256 `85a6d072c1c0e8e753d856ea919890d258e8663e02dbbe11c2e1c6f745baec8c`
- OSV 依赖复核：`yaml@2.8.3` 和 `esbuild@0.25.9` 均无匹配漏洞；`yaml@2.8.1` 的深层 YAML 栈溢出记录已修复。
- Markdown 相对链接：86 个，断链 0（2026-08-20 复核）。
- runtime 边界静态检查：旧 envelope、`trust_context`、`maintainer_ci`、`--evidence`、secret input、网络和运行时安装路径均为 0。

## 仍未满足的公开发布门槛

以下项目没有可验证证据，因此候选仍为 **公开发布 NO-GO**：

1. 真实 maintainer-controlled CI/E2 producer identity。
2. 第三方安全审计。
3. 外部试点及其不包含私有数据的反馈记录。
4. 用户对 reviewed immutable commit、tag、GitHub Release 和 Marketplace 更新的明确授权。

这些门槛不能由本地 synthetic fixture、E1 Receipt 或本地 bundle 哈希替代。获得授权和外部证据后，应在新的发布操作中重新运行全部检查，记录最终 commit/tag、Release body、artifact digest、回滚路径，并保留 v0.1.3 不变。

## 本轮外部状态核对

本轮只读核对了公开 GitHub 状态，没有执行远端写入：

- 公开 `main` 和 `v0.1.3` tag 仍指向 `d82c7863f48878bfee66e978e7569c464de48ea2`。
- 公开 Release/Tags 中没有 `v0.2.0`；现有 Release validation workflow 仍是面向历史 v0.1.3 合同的单一 workflow。
- 最近公开成功 run `32211203587` 的 head 仍是 v0.1.3；它不能证明 v0.2.0 的 Node 24 Action 或 native Receipt 合同。
- GitHub CLI 认证已由 maintainer 完成人工 device login；提升网络权限下 `gh auth status` 已确认 `Fr33man233` 的 `repo` scope。只读 `git push --dry-run` 通过，但没有执行实际 push。
- 远端目前没有 `codex/v0.2.0-local-handoff` 分支，也没有 v0.2 workflow；现有 workflow 仍只固定验证公开 v0.1.3。因此真实 v0.2 maintainer CI/E2 尚未运行，不能把认证成功写成 CI 证明。
- 公开仓库状态中没有第三方安全审计或外部试点证据；不得补造该证据。

本地三态 pilot 已重新执行：`tests/native-runner-state.test.ts` 通过，证明 fresh pass → 覆盖文件后 `AEG003` 过期失败 → 更新 `workspaceAfter`/core digest 后再次通过；该结果仍属于本地 synthetic evidence。

## 回滚

候选没有数据库、ledger、queue、credential 或迁移。发布后如发现问题，消费者应固定回经过复核的 v0.1.3 或完整 immutable commit；不得移动 v0.1.3 tag，不得用移动 alias 掩盖回滚。
