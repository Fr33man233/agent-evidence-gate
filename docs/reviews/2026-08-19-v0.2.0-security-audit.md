# AEG v0.2.0 本地安全审计记录

**日期：** 2026-08-19  
**审计类型：** 隔离工作树本地代码、测试、构建与文档审计；不是第三方安全审计，也不是 maintainer CI/E2 证明。  
**分支：** `codex/v0.2.0-local-handoff`  
**基线：** `ddea99a5a9860f750151f2e28b45fbd077f27b92`  
**公开边界：** v0.1.3、公开 tag、Release、Marketplace alias 和 main 未修改；未执行 push、PR、Release 或 Marketplace 写入。

## 审计结论

- **本地 v0.2.0 RC：GO。** 当前实现对原生 OMK EvidenceReceipt v3、路径边界、Git workspace 状态、隐私、确定性、CLI 和 Node 24 Action 的本地门禁审计通过；未发现未关闭的 P0/P1 问题。
- **公开发布：NO-GO。** v0.2.0 仍未发布；真实 maintainer CI/E2、第三方安全审计和外部试点没有验证。不能将本记录写成公开交付或 real E2 证明。

## 审计范围与冻结边界

审计覆盖 `src/`、`dist/`、`action.yml`、`.github/workflows/`、测试、依赖布局和 v0.2 设计/计划/交接/RC 文档。按冻结设计确认：只读、离线、确定性、无 LLM/API/network/secrets；不执行 Receipt 或 candidate command/package/test/script；不实现 OMK ledger、replay database、receipt hash 生成或执行控制面；Receipt 最高 E1，只有 `local` profile 可通过，`pr`/`protected` 使用 `AEG070` 失败。

## 发现、修复和验证

| 编号 | 严重度 | 发现 | 处理 | 状态 |
| --- | --- | --- | --- | --- |
| AUD-001 | P1 | `runner` 在 Receipt discovery 后直接 `readFileSync(path)`，存在重新解析期间绕过有界/link-safe 读取的 TOCTOU 边界。 | 先加入失败合同测试，再统一使用 `readBoundedFile(path, LIMITS.receiptBytes, "AEG003")`。 | 已修复并通过 |
| AUD-002 | P1 | Receipt 文件位于祖先 junction/symlink 下时，单独检查目标文件不足以阻止 traversal。 | `discoverReceiptPaths` 和普通文件检查逐层调用 `assertNoLinkAncestors`；加入祖先 junction 负例。 | 已修复并通过 |
| AUD-003 | P1 | Git artifact 读取把任意 I/O 错误折叠成 `state: missing`，可能把不可用状态伪装成合法缺失状态；同时未检查 artifact 祖先 link。 | 先加入失败合同和 junction 负例；仅 `ENOENT`/`ENOTDIR` 允许 missing，其他错误转为 `AEG003`，并拒绝祖先 link。另加入合法 missing artifact 回归测试。 | 已修复并通过 |
| AUD-004 | P2 | `package.json` 仍为公开 v0.1.3 版本号，而 v0.2.0 尚未发布。 | 保持不改；这是未发布版本的发布元数据边界，需单独发布授权和版本决策。 | 延后，发布前阻断 |
| AUD-005 | P2 | 真实 maintainer CI/E2、第三方安全审计和外部试点没有证据。 | 保留为公开发布门槛；不以本地测试替代。 | 未验证，发布阻断 |
| AUD-006 | P2 | 非提升权限运行 `pnpm run build` 受当前 Windows esbuild 路径权限布局影响失败。 | 记录环境事实；不重复安装依赖。使用同一已有 `node_modules` 在受控提升权限下完成两次构建并验证字节一致。 | 环境性已缓解，仍需在干净 CI 复核 |

## 运行时边界审计

对 `src`、`action.yml`、`package.json` 和工作流做静态检查，以下运行时/权限模式均为 0：`pull_request_target`、`trust_context`、`maintainer_ci`、旧 `evidencePath`、`--evidence`、secret input、`fetch(`、运行时 `npm/pnpm install`、`shell: true`。唯一生产 child process 是 `src/git.ts` 中固定参数、无 shell、超时、输出上限和环境 blocklist 的 Git `spawnSync`。历史 release-validation workflow 明确固定到公开 v0.1.3 immutable commit，属于历史合同验证，不调用未发布的 v0.2 Action。

## 证据结果

- `node node_modules/typescript/bin/tsc --noEmit`：通过。
- `pnpm test`：30/30 通过，约 2.926 秒。
- `pnpm run build`：同一已有依赖连续两次通过（提升权限），字节一致：
  - `dist/aeg.cjs`：319,827 bytes，SHA-256 `cc19b5a57999d42c50342616ba06029d801f83763d586556997a8c601fa1d958`
  - `dist/action.cjs`：319,063 bytes，SHA-256 `4f0acd64654db759ecd02331b108de06a7dea22f7d4407c585f421bacce80003`
- `git diff --check`：通过；仅有 Git 对 CRLF 的提示。
- Markdown 相对链接检查：78 个链接，断链 0。
- 负例覆盖：未知/重复字段、隐私字段、Receipt digest、workspace stale、scope escape、输入 link/junction、祖先 junction、missing artifact、PR/protected E1、删除的 `--evidence` 和 Action 边界。

## 资源与回滚边界

本次审计未新增生产依赖、未重复安装、未运行候选内容、未发送网络请求。变更仅在当前隔离分支；没有数据库、ledger 或外部状态迁移。若发现 P0 隐私、安全、确定性或数据丢失问题，应停止版本推进并回到最后一个已验证提交；公开回滚仍是切换到已审查的 immutable v0.1.3 commit，不改写公开 tag 或 main。

## 最终门槛

本地 RC **GO**；公开发布 **NO-GO**，直到获得新的明确授权并完成固定 reviewed commit、真实 maintainer CI/E2、第三方安全审计和外部试点。后续产品版本必须新建独立 Codex 任务；本任务不启动 v0.3.0。
