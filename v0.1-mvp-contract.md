# Agent Evidence Gate v0.1 MVP 合同

> 版本：v0.1-draft  
> 日期：2026-08-17  
> 决定：`BOOTSTRAP_CONDITIONAL_GO`  
> 上位文档：[产品立项书.md](./产品立项书.md)、[v0-specification.md](./v0-specification.md)  
> 技术依据：[互操作 Spike ADR](./research/spikes/2026-08-17/adr.md)

## 1. 决策与未验证事项

用户明确决定：暂不进行 3-5 名维护者的使用价值调研，先完成可发布 MVP，再通过 GitHub 公开使用情况决定后续迭代。

因此：

- 采用价值从 v0 的前置 GO 条件降级为发布后的验证任务；
- v0.1 可以开始实现和公开发布；
- 当前 `P5 CI/E2 = unverified` 仍然有效，不能宣传为已完成真实 CI 证明；
- `E2-candidate` 只能表示“满足候选 trust context 规则”，不表示 runner 已被独立证明；
- MVP 通过不等于产品市场成立，也不等于 Codex Fund 会批准申请。

## 2. MVP 目标

交付一个维护者可以在本地或只读 GitHub Action 中运行的完整 V0 功能面 MVP，证明以下闭环可工作：

> 外部证据来源 → AEG canonical profile → 确定性策略判定 → 最小 JSON/Markdown 报告 → required check 可消费的退出码。

MVP 不执行测试、不调用 LLM、不解析自然语言、不运行 PR 中的候选代码。这里的“最小”指交付面最小，不代表删减已冻结的 V0 功能要求。

## 3. 交付范围

| 模块 | v0.1 必须交付 | 明确不交付 |
| --- | --- | --- |
| 核心引擎 | `agent-task.yml`、`agent-trace.jsonl`、canonicalization、Git 事实、assurance、freshness、check、scope、dependency、budget、surface 策略 | 云服务、数据库、队列、后台任务 |
| 证据适配器 | OMK v3 receipt、maintainer CI envelope 两个 adapter | 第三个 agent/harness、自动发现适配器 |
| CLI | 只读 `verify` 命令；JSON、Markdown、稳定退出码 | 交互式 TUI、自动修复、自动提交 |
| GitHub Action | 只读调用核心 verifier；不执行候选 PR 命令；无 secrets | Marketplace 上架、`pull_request_target` 执行 PR 内容 |
| 报告 | gate、assurance、reason codes、remediation codes、限制说明 | prompt、源码、原始命令、cwd、stdout/stderr |
| 测试 | F01-F28 完整 V0 夹具、I00-I15 互操作夹具、确定性回放、隐私 sentinel、Action 静态安全测试 | 大规模 benchmark、真实项目质量结论 |
| 文档 | Quickstart、profile 示例、威胁模型、能力边界、公开 synthetic demo | 宣称已验证市场需求或 E2 生产证明 |
| 发布 | OSI 认可许可证、公开 GitHub 仓库、可复现构建/测试说明、版本标签 | 商业托管、遥测后台、用户数据收集 |

### 3.1 V0 功能完整性继承

以下要求从 [v0-specification.md](./v0-specification.md) 继承为 v0.1 `MUST`，不因采用价值调研延后而删减：

- 声明层保留 C0/C1/C2：C0 自然语言只展示，C1 manifest 结构化要求进入策略，C2 只按稳定 claim ID 关联，禁止自由文本解析；
- assurance 保留 E0/E1/E2/E3 语义；当前真实 E2 生产证明仍标为 `unverified`，但 verifier 必须实现降级和 required assurance 门禁；
- 输入契约包括 task manifest、trace JSONL、Git 事实和外部 producer/trust context；缺失字段不得默认为成功；
- profile 必须支持 `local`、`pr`、`protected`，包括资源缺失、surface 变化和 `approval_required` 语义；
- 策略必须覆盖 AEG001、AEG002、AEG003、AEG010、AEG020、AEG021、AEG022、AEG030、AEG031、AEG040、AEG050、AEG060、AEG061、AEG070；
- 事件完整性必须检查 schema version、run_id、event_id、sequence、事件类型、producer、command receipt、termination、exit code、stdout/stderr 摘要和禁存字段；
- 资源与隐私规则必须覆盖敏感读写、依赖策略、token/时长/工具调用/重试预算、超限和不可观察状态；
- 报告必须分离 `policy_verdict`、`assurance_level`、`required_assurance` 和 `gate_verdict`，并提供 evidence refs、reason/remediation codes 与 limitations；
- I00-I15 只验证转向后的互操作/外部 assurance 组合，不能替代 F01-F28 的完整功能验收。

适配器只负责把 OMK v3 和 maintainer CI 记录映射到这些既有结构；适配器数量减少不等于核心功能减少。

## 4. 技术路线冻结

- 源码语言：TypeScript；发布产物为固定版本、无需用户安装运行时依赖的 Node bundle。
- 运行时：确定性代码，模型调用 0；不把模型输出放入 verdict 路径。
- 依赖：尽量零运行时依赖；新增依赖必须有许可证、锁文件和供应链理由。
- 架构：`adapter → canonical profile → policy engine → report renderer`。
- 适配器字段不得进入核心策略条件分支。
- `E1` 是 producer 可控/自报记录；`E2-candidate` 必须有 receipt 之外的独立 trust context；`E3` 不实现。
- 当前 head、repository、stable check、workflow、run/job、artifact digest 和 policy digest 必须可核对。
- verifier surface 或受保护 workflow 被候选 PR 修改时，默认 fail 或 `approval_required`。

## 5. 安全与隐私边界

- fork PR 和候选 PR 内容只作为不可信数据读取，不执行其中命令、脚本、安装或构建。
- 不使用 secrets，不上传源码、prompt、原始输出或完整命令。
- 公共报告采用字段白名单；错误信息不回显不可信值。
- Action 权限最小化；默认只读；不写 issue、标签、评论或合并状态之外的外部资源。
- 所有 profile、fixture 和示例使用合成数据。
- README 必须明确：AEG 不证明代码正确、测试充分或 runner 永不失陷。

## 6. MVP 验收标准

### 必须通过

1. F01-F28 全部自动化通过，所有故意违规夹具 `false_pass_rate=0`。
2. I00-I15 全部自动化通过，12 个负例不得放行。
3. OMK 与 CI envelope 使用同一完整核心策略，等价输入得到一致核心 verdict。
4. C0 不进入 verdict；C1/C2 只按结构化 ID 关联；E1 自报无法升级为 E2-candidate。
5. `local`、`pr`、`protected` 的最低 assurance、warn/fail、budget 和 approval 语义符合规格。
6. manifest、trace、Git state、required checks、scope、敏感路径、依赖、预算和 verifier/test surface 均有对应检查与负例。
7. stale head、未知 check、非零退出、取消/超时、surface 变化均 fail-closed 或按规格产生 warn/approval_required。
8. 相同输入重复运行输出逐字节一致，`traceability_rate=100%`。
9. 生成的 JSON/Markdown 不包含 sentinel、prompt、源码、command、cwd、stdout/stderr 或 secrets。
10. CLI 在干净目录可按文档运行；退出码可被 CI required check 消费。
11. Action 测试证明它不执行 fixture 中的命令或 PR 脚本。
12. 公开仓库包含许可证、版本、限制、威胁模型、回滚说明和 synthetic demo。
13. 发布前完成一次独立安全/隐私 review，并记录未验证的真实 CI/E2 边界。

### 不作为 v0.1 通过条件

- 维护者采用数量；
- 真实业务仓库数量；
- Codex Fund 批准；
- 代码质量或业务测试覆盖充分性（AEG 的结构化门禁测试仍是必须项）；
- E3 或密码学 attestation；
- 真实 GitHub Actions E2 生产证明（当前标记 `unverified`）。

## 7. 发布后验证计划

发布后不收集隐私遥测，使用公开且用户主动提交的信号：

- GitHub stars、issues、讨论和复现报告；
- synthetic demo 的成功运行记录；
- 用户主动报告的配置时间、误报、漏报和兼容性问题；
- 是否有人在真实仓库启用 required check；
- 不同 harness 的实际适配需求。

这些信号只用于决定 v0.2 是否值得开始，不能事后包装成发布前市场验证。

## 8. 资源、回滚和出口

| 项目 | 约束 |
| --- | --- |
| 模型/API | 运行时 0；开发辅助调用单独记录 |
| 网络 | 测试默认离线；发布构建使用锁定依赖 |
| 代码范围 | 一个核心、两个 adapter、一个 CLI、一个只读 Action |
| 回滚 | 删除/撤回 Action 版本、撤销 GitHub release、保留兼容旧报告解析的版本说明 |
| 主要风险 | 没有前置采用证据；真实 E2 未验证；GitHub Action 安全边界容易被误配 |
| 退出决定 | `MVP_GO`、`MVP_PIVOT` 或 `MVP_NO-GO`；不得把使用量不足解释为技术通过 |

## 9. Senior full-stack v0.1 PRE-FLIGHT

| 评审面 | 决定 | 分类 |
| --- | --- | --- |
| 用户/业务价值 | 采用价值延后，用公开 MVP 使用信号验证 | Risk / Defer |
| 产品范围 | 完整 V0 verifier 语义、两个 adapter、CLI、只读 Action、完整报告契约 | Optimize now |
| UX | Quickstart 五分钟目标作为设计目标，不宣称已验证 | Optimize now |
| 前端 | 无 Dashboard | Remove |
| 后端 | 无服务、数据库和外部写入 | Remove |
| 模型边界 | 运行时不使用 LLM | Optimize now |
| 数据合同 | 采用 Spike candidate profile；正式版本字段需兼容性测试 | Optimize now |
| 安全 | 不执行不可信 PR、不用 secrets、最小 Action 权限 | P0 |
| 测试 | F01-F28 + I00-I15、负例、隐私、确定性、Action 静态安全 | P0 |
| 运维/发布 | 公开仓库、固定版本、可回滚 release | Optimize now |
| 资源效率 | 先完成确定性核心，再做 Action 包装；不扩展第三 adapter | Optimize now |
| 最近外部里程碑 | GitHub 可运行 MVP；随后收集公开使用信号并准备 Fund 材料 | Optimize now |

前置评审决定：**BOOTSTRAP_CONDITIONAL_GO**。允许进入 v0.1 实现；任何 P0 安全/隐私失败立即停止发布，任何“E2 已真实验证”表述必须保持禁用。
